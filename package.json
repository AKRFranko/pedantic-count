{
  "name": "pedantic-count",
  "version": "1.0.2",
  "description": "Overly verbose, indexable, linkable, gherkinable numbers.",
  "main": "index.js",
  "scripts": {
    "pretest": "npm run-script build",
    "test": "./node_modules/.bin/cucumber.js",
    "build": "npx babel ./src/count.js --out-file ./compiled.js && browserify --standalone PedanticCount ./compiled.js -o index.js"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-polyfill": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "cucumber": "^3"
  },
  "author": "franko <code@akr.club> (http://akr.club)",
  "license": "ISC"
}
